version: "2"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
          - errcheck
formatters:
  enable:
    - gofmt

# Used by `make seed` / `make dev` on the host. Matches deploy/docker-compose.dev.yml (Postgres on host port 5432).
DATABASE_URL=postgres://jejak:jejak@localhost:5432/jejak_inderasakti?sslmode=disable
REDIS_URL=redis://localhost:6379/0
JWT_SECRET=dev-only-secret-change-me-dev-only-secret
PORT=8080
ENV=development
LOG_LEVEL=info
